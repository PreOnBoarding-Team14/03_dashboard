### 상수로 사용하는 Data 들은 Util ConstantsSet으로 다 분리
1. Method.ts, Material.ts

### 맨 처음 한번만 쓰는 변수는 제일 위에서 선언 => 리렌더링 될 때 불러 올 필요가 없기 때문
1. Dashboard.tsx => apiParams 변수

### 해당 컴포넌트 밖에서도 사용되는 Props들은 다 한 곳에 정리

### Click 이벤트, Change 이벤트 여러개 만들고 줄이는 방법 생각해보기
예시) Header.tsx -> onClick, onChange 함수 -> 이 방법만이 정답은 아님!! 다르게 만들어도 됨!! 연습니까 이것저것 해보기!

### 변수명 웬만하면 변수 타입 알 수 있게 지정하는 것이 좋다! -> 나중에 알아 보기도 더 편합니다.

### 반복되는 동작들은 함수로 만들어보기
코드 짜다가 흠..아까도 이런 비슷한걸 하고 있었는데...? 싶으면 공통되는 부분을 찾아 빼는 연습을 해봅시다!
예시) Header.tsx -> onChangeMethod, onChangeMaterial의 경우 변경할 state만 다를 뿐 똑같은 동작을 하고있다.

### 자식 컴포넌트에서 setState 지양하기(부모로 부터 받은 Props로 최대한 활용)

### css(styled)와 jsx, tsx 코드는 가급적 분리

**** 처음부터 깔끔하게 하려고 하기보단(물론 그러면 더 좋음, 하지만 아직 어렵고 익숙하지 않으니까요..) 기능 하나 완성 시킨 후 차근차근 고민해보기